<h2>Editing Book</h2>

<%= form_with model: @list, url:update_list_path(@list.id), method: :patch do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Body</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Update Book' %>
  <%= link_to "Show", update_list_path(@list.id) %>


<% end %>